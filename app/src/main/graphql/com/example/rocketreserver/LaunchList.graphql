query LaunchList($cursor: String) {
  launchConnection: launches(after: $cursor) {
    launches {
      id
      site
      isBooked
      mission {
        name
        missionPatch(size: SMALL)
      }
    }
  }
}